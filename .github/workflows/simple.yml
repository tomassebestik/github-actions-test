name: Shell Commands

on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: Echo something
              run: eccho "This is something"

            - name: Multiline script
              run: |
                node -v
                npm -v
